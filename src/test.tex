\documentclass[tikz]{standalone}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}


\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        xbar, xmin=0,
        xlabel style={align=center},
        xlabel={\textbf{Execution time in seconds} \\ (smaller is better)},
        symbolic y coords={Bash,Python,Java,JavaScript,Haskell,C},
        ytick=data,
        nodes near coords,
        nodes near coords align={horizontal},
        ytick=data,
        xmajorgrids,
        xbar=2*\pgflinewidth,
    ]
        \addplot[style={fill=blue}] coordinates {
            (0.2,Bash)
            (0.4,Python)
            (3.4,Java)
            (0.1,JavaScript)
            (2.1,Haskell)
            (2.1,C)
        };

        \addplot[style={fill=ugent-ps}] coordinates {
            (0.5,Bash)
            (0.8,Python)
            (1.9,Java)
            (0.8,JavaScript)
            (1.9,Haskell)
            (0.6,C)
        };
    \end{axis}
\end{tikzpicture}

\end{document}
